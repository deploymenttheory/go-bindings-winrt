// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package store

import (
	"unsafe"

	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
)

// CurrentApp returns the Windows.ApplicationModel.Store.ICurrentApp statics of the
// Windows.ApplicationModel.Store.CurrentApp runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentApp() (*ICurrentApp, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentApp", &IID_ICurrentApp)
	if err != nil {
		return nil, err
	}
	return (*ICurrentApp)(unsafe.Pointer(factory)), nil
}

// CurrentApp2Statics returns the Windows.ApplicationModel.Store.ICurrentApp2Statics statics of the
// Windows.ApplicationModel.Store.CurrentApp runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentApp2Statics() (*ICurrentApp2Statics, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentApp", &IID_ICurrentApp2Statics)
	if err != nil {
		return nil, err
	}
	return (*ICurrentApp2Statics)(unsafe.Pointer(factory)), nil
}

// CurrentAppStaticsWithFiltering returns the Windows.ApplicationModel.Store.ICurrentAppStaticsWithFiltering statics of the
// Windows.ApplicationModel.Store.CurrentApp runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppStaticsWithFiltering() (*ICurrentAppStaticsWithFiltering, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentApp", &IID_ICurrentAppStaticsWithFiltering)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppStaticsWithFiltering)(unsafe.Pointer(factory)), nil
}

// CurrentAppWithCampaignId returns the Windows.ApplicationModel.Store.ICurrentAppWithCampaignId statics of the
// Windows.ApplicationModel.Store.CurrentApp runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppWithCampaignId() (*ICurrentAppWithCampaignId, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentApp", &IID_ICurrentAppWithCampaignId)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppWithCampaignId)(unsafe.Pointer(factory)), nil
}

// CurrentAppWithConsumables returns the Windows.ApplicationModel.Store.ICurrentAppWithConsumables statics of the
// Windows.ApplicationModel.Store.CurrentApp runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppWithConsumables() (*ICurrentAppWithConsumables, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentApp", &IID_ICurrentAppWithConsumables)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppWithConsumables)(unsafe.Pointer(factory)), nil
}

// CurrentAppSimulator returns the Windows.ApplicationModel.Store.ICurrentAppSimulator statics of the
// Windows.ApplicationModel.Store.CurrentAppSimulator runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppSimulator() (*ICurrentAppSimulator, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentAppSimulator", &IID_ICurrentAppSimulator)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppSimulator)(unsafe.Pointer(factory)), nil
}

// CurrentAppSimulatorStaticsWithFiltering returns the Windows.ApplicationModel.Store.ICurrentAppSimulatorStaticsWithFiltering statics of the
// Windows.ApplicationModel.Store.CurrentAppSimulator runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppSimulatorStaticsWithFiltering() (*ICurrentAppSimulatorStaticsWithFiltering, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentAppSimulator", &IID_ICurrentAppSimulatorStaticsWithFiltering)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppSimulatorStaticsWithFiltering)(unsafe.Pointer(factory)), nil
}

// CurrentAppSimulatorWithCampaignId returns the Windows.ApplicationModel.Store.ICurrentAppSimulatorWithCampaignId statics of the
// Windows.ApplicationModel.Store.CurrentAppSimulator runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppSimulatorWithCampaignId() (*ICurrentAppSimulatorWithCampaignId, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentAppSimulator", &IID_ICurrentAppSimulatorWithCampaignId)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppSimulatorWithCampaignId)(unsafe.Pointer(factory)), nil
}

// CurrentAppSimulatorWithConsumables returns the Windows.ApplicationModel.Store.ICurrentAppSimulatorWithConsumables statics of the
// Windows.ApplicationModel.Store.CurrentAppSimulator runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func CurrentAppSimulatorWithConsumables() (*ICurrentAppSimulatorWithConsumables, error) {
	factory, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.CurrentAppSimulator", &IID_ICurrentAppSimulatorWithConsumables)
	if err != nil {
		return nil, err
	}
	return (*ICurrentAppSimulatorWithConsumables)(unsafe.Pointer(factory)), nil
}

// LicenseInformation is the Windows.ApplicationModel.Store.LicenseInformation runtime class, surfaced through its
// default interface ILicenseInformation. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type LicenseInformation struct {
	ILicenseInformation
}

// ListingInformation is the Windows.ApplicationModel.Store.ListingInformation runtime class, surfaced through its
// default interface IListingInformation. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type ListingInformation struct {
	IListingInformation
}

// AsListingInformation2 queries the instance's IListingInformation2 interface.
// The returned reference is owned by the caller.
func (self *ListingInformation) AsListingInformation2() (*IListingInformation2, error) {
	return winrt.QueryInterface[IListingInformation2](unsafe.Pointer(self), &IID_IListingInformation2)
}

// ProductLicense is the Windows.ApplicationModel.Store.ProductLicense runtime class, surfaced through its
// default interface IProductLicense. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type ProductLicense struct {
	IProductLicense
}

// AsProductLicenseWithFulfillment queries the instance's IProductLicenseWithFulfillment interface.
// The returned reference is owned by the caller.
func (self *ProductLicense) AsProductLicenseWithFulfillment() (*IProductLicenseWithFulfillment, error) {
	return winrt.QueryInterface[IProductLicenseWithFulfillment](unsafe.Pointer(self), &IID_IProductLicenseWithFulfillment)
}

// ProductListing is the Windows.ApplicationModel.Store.ProductListing runtime class, surfaced through its
// default interface IProductListing. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type ProductListing struct {
	IProductListing
}

// AsProductListingWithMetadata queries the instance's IProductListingWithMetadata interface.
// The returned reference is owned by the caller.
func (self *ProductListing) AsProductListingWithMetadata() (*IProductListingWithMetadata, error) {
	return winrt.QueryInterface[IProductListingWithMetadata](unsafe.Pointer(self), &IID_IProductListingWithMetadata)
}

// AsProductListing2 queries the instance's IProductListing2 interface.
// The returned reference is owned by the caller.
func (self *ProductListing) AsProductListing2() (*IProductListing2, error) {
	return winrt.QueryInterface[IProductListing2](unsafe.Pointer(self), &IID_IProductListing2)
}

// ProductPurchaseDisplayProperties is the Windows.ApplicationModel.Store.ProductPurchaseDisplayProperties runtime class, surfaced through its
// default interface IProductPurchaseDisplayProperties. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type ProductPurchaseDisplayProperties struct {
	IProductPurchaseDisplayProperties
}

// NewProductPurchaseDisplayProperties activates Windows.ApplicationModel.Store.ProductPurchaseDisplayProperties through its default
// constructor.
func NewProductPurchaseDisplayProperties() (*ProductPurchaseDisplayProperties, error) {
	instance, err := winrt.ActivateInstance("Windows.ApplicationModel.Store.ProductPurchaseDisplayProperties")
	if err != nil {
		return nil, err
	}
	defer instance.Release()
	return winrt.QueryInterface[ProductPurchaseDisplayProperties](unsafe.Pointer(instance), &IID_IProductPurchaseDisplayProperties)
}

// CreateProductPurchaseDisplayProperties constructs a Windows.ApplicationModel.Store.ProductPurchaseDisplayProperties instance through
// Windows.ApplicationModel.Store.IProductPurchaseDisplayPropertiesFactory.CreateProductPurchaseDisplayProperties. The activation factory is fetched
// per call (a factory cache is a future optimization).
func CreateProductPurchaseDisplayProperties(name string) (*ProductPurchaseDisplayProperties, error) {
	factoryUnknown, err := winrt.GetActivationFactory("Windows.ApplicationModel.Store.ProductPurchaseDisplayProperties", &IID_IProductPurchaseDisplayPropertiesFactory)
	if err != nil {
		return nil, err
	}
	factory := (*IProductPurchaseDisplayPropertiesFactory)(unsafe.Pointer(factoryUnknown))
	defer factory.Release()
	instance, err := factory.CreateProductPurchaseDisplayProperties(name)
	if err != nil {
		return nil, err
	}
	return (*ProductPurchaseDisplayProperties)(unsafe.Pointer(instance)), nil
}

// PurchaseResults is the Windows.ApplicationModel.Store.PurchaseResults runtime class, surfaced through its
// default interface IPurchaseResults. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type PurchaseResults struct {
	IPurchaseResults
}

// UnfulfilledConsumable is the Windows.ApplicationModel.Store.UnfulfilledConsumable runtime class, surfaced through its
// default interface IUnfulfilledConsumable. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type UnfulfilledConsumable struct {
	IUnfulfilledConsumable
}
