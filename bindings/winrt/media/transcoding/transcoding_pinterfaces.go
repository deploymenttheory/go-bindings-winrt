// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package transcoding

import (
	"syscall"
	"unsafe"

	"github.com/deploymenttheory/go-bindings-win32/bindings/runtime/win32"
	syswinrt "github.com/deploymenttheory/go-bindings-win32/bindings/win32/system/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/winrt/foundation"
)

// IAsyncActionWithProgressOfDouble is the WinRT interface Windows.Foundation.IAsyncActionWithProgress`1<Double>.
// IID: 4f1430a6-a825-56ca-b047-1a9bad52ba67
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncActionWithProgressOfDouble struct {
	syswinrt.IInspectable
}

// IID_IAsyncActionWithProgressOfDouble is the interface identifier for IAsyncActionWithProgressOfDouble.
var IID_IAsyncActionWithProgressOfDouble = win32.GUID{Data1: 0x4f1430a6, Data2: 0xa825, Data3: 0x56ca, Data4: [8]byte{0xb0, 0x47, 0x1a, 0x9b, 0xad, 0x52, 0xba, 0x67}}

// slot 6: put_Progress skipped: parameterized type Windows.Foundation.AsyncActionProgressHandler`1

// slot 7: get_Progress skipped: parameterized type Windows.Foundation.AsyncActionProgressHandler`1

// SetCompleted (propput put_Completed) dispatches through IAsyncActionWithProgressOfDouble's vtable slot 8.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncActionWithProgressOfDouble) SetCompleted(handler *AsyncActionWithProgressCompletedHandlerOfDouble) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 9: get_Completed skipped: parameterized type Windows.Foundation.AsyncActionWithProgressCompletedHandler`1

// GetResults dispatches through IAsyncActionWithProgressOfDouble's vtable slot 10.
func (self *IAsyncActionWithProgressOfDouble) GetResults() error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[10], uintptr(unsafe.Pointer(self)))
	return win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncActionWithProgressOfDouble reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncActionWithProgressOfDouble) Await() error {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncActionWithProgressCompletedHandlerOfDouble(func(_ *IAsyncActionWithProgressOfDouble, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return err
		}
		return winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfPrepareTranscodeResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Media.Transcoding.PrepareTranscodeResult>.
// IID: f5f07c13-3047-5bab-8eb7-6e5d7d14afae
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfPrepareTranscodeResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfPrepareTranscodeResult is the interface identifier for IAsyncOperationOfPrepareTranscodeResult.
var IID_IAsyncOperationOfPrepareTranscodeResult = win32.GUID{Data1: 0xf5f07c13, Data2: 0x3047, Data3: 0x5bab, Data4: [8]byte{0x8e, 0xb7, 0x6e, 0x5d, 0x7d, 0x14, 0xaf, 0xae}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfPrepareTranscodeResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfPrepareTranscodeResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfPrepareTranscodeResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfPrepareTranscodeResult's vtable slot 8.
func (self *IAsyncOperationOfPrepareTranscodeResult) GetResults() (*IPrepareTranscodeResult, error) {
	result := new(*IPrepareTranscodeResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfPrepareTranscodeResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfPrepareTranscodeResult) Await() (*IPrepareTranscodeResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfPrepareTranscodeResult(func(_ *IAsyncOperationOfPrepareTranscodeResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}
