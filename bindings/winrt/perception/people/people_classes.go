// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package people

import (
	"unsafe"

	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
)

// EyesPose is the Windows.Perception.People.EyesPose runtime class, surfaced through its
// default interface IEyesPose. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type EyesPose struct {
	IEyesPose
}

// EyesPoseStatics returns the Windows.Perception.People.IEyesPoseStatics statics of the
// Windows.Perception.People.EyesPose runtime class. The activation factory is queried for
// the statics IID directly, so the returned reference (owned by the caller;
// Release when done) is the statics interface itself.
func EyesPoseStatics() (*IEyesPoseStatics, error) {
	factory, err := winrt.GetActivationFactory("Windows.Perception.People.EyesPose", &IID_IEyesPoseStatics)
	if err != nil {
		return nil, err
	}
	return (*IEyesPoseStatics)(unsafe.Pointer(factory)), nil
}

// HandMeshObserver is the Windows.Perception.People.HandMeshObserver runtime class, surfaced through its
// default interface IHandMeshObserver. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type HandMeshObserver struct {
	IHandMeshObserver
}

// HandMeshVertexState is the Windows.Perception.People.HandMeshVertexState runtime class, surfaced through its
// default interface IHandMeshVertexState. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type HandMeshVertexState struct {
	IHandMeshVertexState
}

// HandPose is the Windows.Perception.People.HandPose runtime class, surfaced through its
// default interface IHandPose. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type HandPose struct {
	IHandPose
}

// HeadPose is the Windows.Perception.People.HeadPose runtime class, surfaced through its
// default interface IHeadPose. Release when done (promoted from
// the embedded IInspectable → IUnknown chain).
type HeadPose struct {
	IHeadPose
}
