// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package midi

import (
	"syscall"
	"unsafe"

	"github.com/deploymenttheory/go-bindings-win32/bindings/runtime/win32"
	syswinrt "github.com/deploymenttheory/go-bindings-win32/bindings/win32/system/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/winrt/foundation"
)

// IAsyncOperationOfIMidiOutPort is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Midi.IMidiOutPort>.
// IID: 32699a4d-1cc0-5a1c-9da6-875197875086
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfIMidiOutPort struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfIMidiOutPort is the interface identifier for IAsyncOperationOfIMidiOutPort.
var IID_IAsyncOperationOfIMidiOutPort = win32.GUID{Data1: 0x32699a4d, Data2: 0x1cc0, Data3: 0x5a1c, Data4: [8]byte{0x9d, 0xa6, 0x87, 0x51, 0x97, 0x87, 0x50, 0x86}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfIMidiOutPort's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfIMidiOutPort) SetCompleted(handler *AsyncOperationCompletedHandlerOfIMidiOutPort) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfIMidiOutPort's vtable slot 8.
func (self *IAsyncOperationOfIMidiOutPort) GetResults() (*IMidiOutPort, error) {
	result := new(*IMidiOutPort)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfIMidiOutPort reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfIMidiOutPort) Await() (*IMidiOutPort, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfIMidiOutPort(func(_ *IAsyncOperationOfIMidiOutPort, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfMidiInPort is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Midi.MidiInPort>.
// IID: cc664f0e-edb1-55c8-9ef7-ec9007e4561c
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfMidiInPort struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfMidiInPort is the interface identifier for IAsyncOperationOfMidiInPort.
var IID_IAsyncOperationOfMidiInPort = win32.GUID{Data1: 0xcc664f0e, Data2: 0xedb1, Data3: 0x55c8, Data4: [8]byte{0x9e, 0xf7, 0xec, 0x90, 0x07, 0xe4, 0x56, 0x1c}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfMidiInPort's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfMidiInPort) SetCompleted(handler *AsyncOperationCompletedHandlerOfMidiInPort) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfMidiInPort's vtable slot 8.
func (self *IAsyncOperationOfMidiInPort) GetResults() (*IMidiInPort, error) {
	result := new(*IMidiInPort)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfMidiInPort reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfMidiInPort) Await() (*IMidiInPort, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfMidiInPort(func(_ *IAsyncOperationOfMidiInPort, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfMidiSynthesizer is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Midi.MidiSynthesizer>.
// IID: 9388b978-13f1-5e37-8133-94430d90dd50
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfMidiSynthesizer struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfMidiSynthesizer is the interface identifier for IAsyncOperationOfMidiSynthesizer.
var IID_IAsyncOperationOfMidiSynthesizer = win32.GUID{Data1: 0x9388b978, Data2: 0x13f1, Data3: 0x5e37, Data4: [8]byte{0x81, 0x33, 0x94, 0x43, 0x0d, 0x90, 0xdd, 0x50}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfMidiSynthesizer's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfMidiSynthesizer) SetCompleted(handler *AsyncOperationCompletedHandlerOfMidiSynthesizer) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfMidiSynthesizer's vtable slot 8.
func (self *IAsyncOperationOfMidiSynthesizer) GetResults() (*IMidiSynthesizer, error) {
	result := new(*IMidiSynthesizer)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfMidiSynthesizer reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfMidiSynthesizer) Await() (*IMidiSynthesizer, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfMidiSynthesizer(func(_ *IAsyncOperationOfMidiSynthesizer, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}
