// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package genericattributeprofile

import (
	"syscall"
	"unsafe"

	"github.com/deploymenttheory/go-bindings-win32/bindings/runtime/win32"
	syswinrt "github.com/deploymenttheory/go-bindings-win32/bindings/win32/system/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
	devicesenumeration "github.com/deploymenttheory/go-bindings-winrt/bindings/winrt/devices/enumeration"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/winrt/foundation"
)

// IAsyncOperationOfDeviceAccessStatus is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Enumeration.DeviceAccessStatus>.
// IID: c00bc2f2-a7f8-5f3f-80d1-2808ef6bca10
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfDeviceAccessStatus struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfDeviceAccessStatus is the interface identifier for IAsyncOperationOfDeviceAccessStatus.
var IID_IAsyncOperationOfDeviceAccessStatus = win32.GUID{Data1: 0xc00bc2f2, Data2: 0xa7f8, Data3: 0x5f3f, Data4: [8]byte{0x80, 0xd1, 0x28, 0x08, 0xef, 0x6b, 0xca, 0x10}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfDeviceAccessStatus's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfDeviceAccessStatus) SetCompleted(handler *AsyncOperationCompletedHandlerOfDeviceAccessStatus) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfDeviceAccessStatus's vtable slot 8.
func (self *IAsyncOperationOfDeviceAccessStatus) GetResults() (devicesenumeration.DeviceAccessStatus, error) {
	result := new(devicesenumeration.DeviceAccessStatus)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfDeviceAccessStatus reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfDeviceAccessStatus) Await() (devicesenumeration.DeviceAccessStatus, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfDeviceAccessStatus(func(_ *IAsyncOperationOfDeviceAccessStatus, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return 0, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return 0, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return 0, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return 0, err
		}
		return 0, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattCharacteristicsResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristicsResult>.
// IID: 0972194a-ac1c-5536-9886-27e58a18f273
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattCharacteristicsResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattCharacteristicsResult is the interface identifier for IAsyncOperationOfGattCharacteristicsResult.
var IID_IAsyncOperationOfGattCharacteristicsResult = win32.GUID{Data1: 0x0972194a, Data2: 0xac1c, Data3: 0x5536, Data4: [8]byte{0x98, 0x86, 0x27, 0xe5, 0x8a, 0x18, 0xf2, 0x73}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattCharacteristicsResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattCharacteristicsResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattCharacteristicsResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattCharacteristicsResult's vtable slot 8.
func (self *IAsyncOperationOfGattCharacteristicsResult) GetResults() (*IGattCharacteristicsResult, error) {
	result := new(*IGattCharacteristicsResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattCharacteristicsResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattCharacteristicsResult) Await() (*IGattCharacteristicsResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattCharacteristicsResult(func(_ *IAsyncOperationOfGattCharacteristicsResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattClientNotificationResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>.
// IID: de27c5cf-6227-5829-b997-88e575ad0680
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattClientNotificationResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattClientNotificationResult is the interface identifier for IAsyncOperationOfGattClientNotificationResult.
var IID_IAsyncOperationOfGattClientNotificationResult = win32.GUID{Data1: 0xde27c5cf, Data2: 0x6227, Data3: 0x5829, Data4: [8]byte{0xb9, 0x97, 0x88, 0xe5, 0x75, 0xad, 0x06, 0x80}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattClientNotificationResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattClientNotificationResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattClientNotificationResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattClientNotificationResult's vtable slot 8.
func (self *IAsyncOperationOfGattClientNotificationResult) GetResults() (*IGattClientNotificationResult, error) {
	result := new(*IGattClientNotificationResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattClientNotificationResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattClientNotificationResult) Await() (*IGattClientNotificationResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattClientNotificationResult(func(_ *IAsyncOperationOfGattClientNotificationResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattCommunicationStatus is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCommunicationStatus>.
// IID: 3ff69516-1bfb-52e9-9ee6-e5cdb78e1683
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattCommunicationStatus struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattCommunicationStatus is the interface identifier for IAsyncOperationOfGattCommunicationStatus.
var IID_IAsyncOperationOfGattCommunicationStatus = win32.GUID{Data1: 0x3ff69516, Data2: 0x1bfb, Data3: 0x52e9, Data4: [8]byte{0x9e, 0xe6, 0xe5, 0xcd, 0xb7, 0x8e, 0x16, 0x83}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattCommunicationStatus's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattCommunicationStatus) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattCommunicationStatus) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattCommunicationStatus's vtable slot 8.
func (self *IAsyncOperationOfGattCommunicationStatus) GetResults() (GattCommunicationStatus, error) {
	result := new(GattCommunicationStatus)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattCommunicationStatus reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattCommunicationStatus) Await() (GattCommunicationStatus, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattCommunicationStatus(func(_ *IAsyncOperationOfGattCommunicationStatus, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return 0, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return 0, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return 0, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return 0, err
		}
		return 0, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattDescriptorsResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptorsResult>.
// IID: ceaf40c7-be37-52a5-9a1b-63398513e597
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattDescriptorsResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattDescriptorsResult is the interface identifier for IAsyncOperationOfGattDescriptorsResult.
var IID_IAsyncOperationOfGattDescriptorsResult = win32.GUID{Data1: 0xceaf40c7, Data2: 0xbe37, Data3: 0x52a5, Data4: [8]byte{0x9a, 0x1b, 0x63, 0x39, 0x85, 0x13, 0xe5, 0x97}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattDescriptorsResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattDescriptorsResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattDescriptorsResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattDescriptorsResult's vtable slot 8.
func (self *IAsyncOperationOfGattDescriptorsResult) GetResults() (*IGattDescriptorsResult, error) {
	result := new(*IGattDescriptorsResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattDescriptorsResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattDescriptorsResult) Await() (*IGattDescriptorsResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattDescriptorsResult(func(_ *IAsyncOperationOfGattDescriptorsResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattDeviceService is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>.
// IID: e5e90272-408f-5055-9bd3-88408982d301
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattDeviceService struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattDeviceService is the interface identifier for IAsyncOperationOfGattDeviceService.
var IID_IAsyncOperationOfGattDeviceService = win32.GUID{Data1: 0xe5e90272, Data2: 0x408f, Data3: 0x5055, Data4: [8]byte{0x9b, 0xd3, 0x88, 0x40, 0x89, 0x82, 0xd3, 0x01}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattDeviceService's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattDeviceService) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattDeviceService) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattDeviceService's vtable slot 8.
func (self *IAsyncOperationOfGattDeviceService) GetResults() (*IGattDeviceService, error) {
	result := new(*IGattDeviceService)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattDeviceService reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattDeviceService) Await() (*IGattDeviceService, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattDeviceService(func(_ *IAsyncOperationOfGattDeviceService, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattDeviceServicesResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceServicesResult>.
// IID: e7c667f6-e874-500f-86ff-760ca6f07a58
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattDeviceServicesResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattDeviceServicesResult is the interface identifier for IAsyncOperationOfGattDeviceServicesResult.
var IID_IAsyncOperationOfGattDeviceServicesResult = win32.GUID{Data1: 0xe7c667f6, Data2: 0xe874, Data3: 0x500f, Data4: [8]byte{0x86, 0xff, 0x76, 0x0c, 0xa6, 0xf0, 0x7a, 0x58}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattDeviceServicesResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattDeviceServicesResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattDeviceServicesResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattDeviceServicesResult's vtable slot 8.
func (self *IAsyncOperationOfGattDeviceServicesResult) GetResults() (*IGattDeviceServicesResult, error) {
	result := new(*IGattDeviceServicesResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattDeviceServicesResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattDeviceServicesResult) Await() (*IGattDeviceServicesResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattDeviceServicesResult(func(_ *IAsyncOperationOfGattDeviceServicesResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattLocalCharacteristicResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristicResult>.
// IID: 1f97164e-88d5-567d-90f9-75d4f6455274
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattLocalCharacteristicResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattLocalCharacteristicResult is the interface identifier for IAsyncOperationOfGattLocalCharacteristicResult.
var IID_IAsyncOperationOfGattLocalCharacteristicResult = win32.GUID{Data1: 0x1f97164e, Data2: 0x88d5, Data3: 0x567d, Data4: [8]byte{0x90, 0xf9, 0x75, 0xd4, 0xf6, 0x45, 0x52, 0x74}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattLocalCharacteristicResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattLocalCharacteristicResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattLocalCharacteristicResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattLocalCharacteristicResult's vtable slot 8.
func (self *IAsyncOperationOfGattLocalCharacteristicResult) GetResults() (*IGattLocalCharacteristicResult, error) {
	result := new(*IGattLocalCharacteristicResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattLocalCharacteristicResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattLocalCharacteristicResult) Await() (*IGattLocalCharacteristicResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattLocalCharacteristicResult(func(_ *IAsyncOperationOfGattLocalCharacteristicResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattLocalDescriptorResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptorResult>.
// IID: 3ef6d808-754f-5040-97ac-0703309c574f
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattLocalDescriptorResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattLocalDescriptorResult is the interface identifier for IAsyncOperationOfGattLocalDescriptorResult.
var IID_IAsyncOperationOfGattLocalDescriptorResult = win32.GUID{Data1: 0x3ef6d808, Data2: 0x754f, Data3: 0x5040, Data4: [8]byte{0x97, 0xac, 0x07, 0x03, 0x30, 0x9c, 0x57, 0x4f}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattLocalDescriptorResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattLocalDescriptorResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattLocalDescriptorResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattLocalDescriptorResult's vtable slot 8.
func (self *IAsyncOperationOfGattLocalDescriptorResult) GetResults() (*IGattLocalDescriptorResult, error) {
	result := new(*IGattLocalDescriptorResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattLocalDescriptorResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattLocalDescriptorResult) Await() (*IGattLocalDescriptorResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattLocalDescriptorResult(func(_ *IAsyncOperationOfGattLocalDescriptorResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattOpenStatus is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattOpenStatus>.
// IID: 983201ac-8777-53e8-86e0-63fa684be1bd
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattOpenStatus struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattOpenStatus is the interface identifier for IAsyncOperationOfGattOpenStatus.
var IID_IAsyncOperationOfGattOpenStatus = win32.GUID{Data1: 0x983201ac, Data2: 0x8777, Data3: 0x53e8, Data4: [8]byte{0x86, 0xe0, 0x63, 0xfa, 0x68, 0x4b, 0xe1, 0xbd}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattOpenStatus's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattOpenStatus) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattOpenStatus) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattOpenStatus's vtable slot 8.
func (self *IAsyncOperationOfGattOpenStatus) GetResults() (GattOpenStatus, error) {
	result := new(GattOpenStatus)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattOpenStatus reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattOpenStatus) Await() (GattOpenStatus, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattOpenStatus(func(_ *IAsyncOperationOfGattOpenStatus, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return 0, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return 0, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return 0, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return 0, err
		}
		return 0, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattReadClientCharacteristicConfigurationDescriptorResult>.
// IID: cf4444cc-4077-5719-8366-46e86b983685
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult is the interface identifier for IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult.
var IID_IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult = win32.GUID{Data1: 0xcf4444cc, Data2: 0x4077, Data3: 0x5719, Data4: [8]byte{0x83, 0x66, 0x46, 0xe8, 0x6b, 0x98, 0x36, 0x85}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattReadClientCharacteristicConfigurationDescriptorResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult's vtable slot 8.
func (self *IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult) GetResults() (*IGattReadClientCharacteristicConfigurationDescriptorResult, error) {
	result := new(*IGattReadClientCharacteristicConfigurationDescriptorResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult) Await() (*IGattReadClientCharacteristicConfigurationDescriptorResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattReadClientCharacteristicConfigurationDescriptorResult(func(_ *IAsyncOperationOfGattReadClientCharacteristicConfigurationDescriptorResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattReadRequest is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattReadRequest>.
// IID: 4732cec2-d943-5ceb-8281-8d54a21b9a45
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattReadRequest struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattReadRequest is the interface identifier for IAsyncOperationOfGattReadRequest.
var IID_IAsyncOperationOfGattReadRequest = win32.GUID{Data1: 0x4732cec2, Data2: 0xd943, Data3: 0x5ceb, Data4: [8]byte{0x82, 0x81, 0x8d, 0x54, 0xa2, 0x1b, 0x9a, 0x45}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattReadRequest's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattReadRequest) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattReadRequest) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattReadRequest's vtable slot 8.
func (self *IAsyncOperationOfGattReadRequest) GetResults() (*IGattReadRequest, error) {
	result := new(*IGattReadRequest)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattReadRequest reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattReadRequest) Await() (*IGattReadRequest, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattReadRequest(func(_ *IAsyncOperationOfGattReadRequest, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattReadResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattReadResult>.
// IID: d40432a8-1e14-51d0-b49b-ae2ce1aa05e5
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattReadResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattReadResult is the interface identifier for IAsyncOperationOfGattReadResult.
var IID_IAsyncOperationOfGattReadResult = win32.GUID{Data1: 0xd40432a8, Data2: 0x1e14, Data3: 0x51d0, Data4: [8]byte{0xb4, 0x9b, 0xae, 0x2c, 0xe1, 0xaa, 0x05, 0xe5}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattReadResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattReadResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattReadResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattReadResult's vtable slot 8.
func (self *IAsyncOperationOfGattReadResult) GetResults() (*IGattReadResult, error) {
	result := new(*IGattReadResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattReadResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattReadResult) Await() (*IGattReadResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattReadResult(func(_ *IAsyncOperationOfGattReadResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattServiceProviderResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattServiceProviderResult>.
// IID: 21781028-f5a2-5d99-a5ab-bce6554fbc02
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattServiceProviderResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattServiceProviderResult is the interface identifier for IAsyncOperationOfGattServiceProviderResult.
var IID_IAsyncOperationOfGattServiceProviderResult = win32.GUID{Data1: 0x21781028, Data2: 0xf5a2, Data3: 0x5d99, Data4: [8]byte{0xa5, 0xab, 0xbc, 0xe6, 0x55, 0x4f, 0xbc, 0x02}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattServiceProviderResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattServiceProviderResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattServiceProviderResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattServiceProviderResult's vtable slot 8.
func (self *IAsyncOperationOfGattServiceProviderResult) GetResults() (*IGattServiceProviderResult, error) {
	result := new(*IGattServiceProviderResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattServiceProviderResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattServiceProviderResult) Await() (*IGattServiceProviderResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattServiceProviderResult(func(_ *IAsyncOperationOfGattServiceProviderResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattSession is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSession>.
// IID: 6d40b467-46b9-516f-8208-db23b786ea48
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattSession struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattSession is the interface identifier for IAsyncOperationOfGattSession.
var IID_IAsyncOperationOfGattSession = win32.GUID{Data1: 0x6d40b467, Data2: 0x46b9, Data3: 0x516f, Data4: [8]byte{0x82, 0x08, 0xdb, 0x23, 0xb7, 0x86, 0xea, 0x48}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattSession's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattSession) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattSession) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattSession's vtable slot 8.
func (self *IAsyncOperationOfGattSession) GetResults() (*IGattSession, error) {
	result := new(*IGattSession)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattSession reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattSession) Await() (*IGattSession, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattSession(func(_ *IAsyncOperationOfGattSession, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattWriteRequest is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattWriteRequest>.
// IID: fb8b3c18-2f60-5b43-b773-146045816e03
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattWriteRequest struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattWriteRequest is the interface identifier for IAsyncOperationOfGattWriteRequest.
var IID_IAsyncOperationOfGattWriteRequest = win32.GUID{Data1: 0xfb8b3c18, Data2: 0x2f60, Data3: 0x5b43, Data4: [8]byte{0xb7, 0x73, 0x14, 0x60, 0x45, 0x81, 0x6e, 0x03}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattWriteRequest's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattWriteRequest) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattWriteRequest) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattWriteRequest's vtable slot 8.
func (self *IAsyncOperationOfGattWriteRequest) GetResults() (*IGattWriteRequest, error) {
	result := new(*IGattWriteRequest)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattWriteRequest reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattWriteRequest) Await() (*IGattWriteRequest, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattWriteRequest(func(_ *IAsyncOperationOfGattWriteRequest, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfGattWriteResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattWriteResult>.
// IID: e83b4534-bd14-5a9b-a53b-17cc02a2a8a8
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfGattWriteResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfGattWriteResult is the interface identifier for IAsyncOperationOfGattWriteResult.
var IID_IAsyncOperationOfGattWriteResult = win32.GUID{Data1: 0xe83b4534, Data2: 0xbd14, Data3: 0x5a9b, Data4: [8]byte{0xa5, 0x3b, 0x17, 0xcc, 0x02, 0xa2, 0xa8, 0xa8}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfGattWriteResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfGattWriteResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfGattWriteResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfGattWriteResult's vtable slot 8.
func (self *IAsyncOperationOfGattWriteResult) GetResults() (*IGattWriteResult, error) {
	result := new(*IGattWriteResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfGattWriteResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfGattWriteResult) Await() (*IGattWriteResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfGattWriteResult(func(_ *IAsyncOperationOfGattWriteResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IAsyncOperationOfIVectorViewOfGattClientNotificationResult is the WinRT interface Windows.Foundation.IAsyncOperation`1<Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>>.
// IID: b6fa5848-accd-536b-a37e-2444d86f2c1f
// Requires: Windows.Foundation.IAsyncInfo.
type IAsyncOperationOfIVectorViewOfGattClientNotificationResult struct {
	syswinrt.IInspectable
}

// IID_IAsyncOperationOfIVectorViewOfGattClientNotificationResult is the interface identifier for IAsyncOperationOfIVectorViewOfGattClientNotificationResult.
var IID_IAsyncOperationOfIVectorViewOfGattClientNotificationResult = win32.GUID{Data1: 0xb6fa5848, Data2: 0xaccd, Data3: 0x536b, Data4: [8]byte{0xa3, 0x7e, 0x24, 0x44, 0xd8, 0x6f, 0x2c, 0x1f}}

// SetCompleted (propput put_Completed) dispatches through IAsyncOperationOfIVectorViewOfGattClientNotificationResult's vtable slot 6.
// A nil handler passes NULL at the ABI (WinRT accepts it where a handler may be cleared).
func (self *IAsyncOperationOfIVectorViewOfGattClientNotificationResult) SetCompleted(handler *AsyncOperationCompletedHandlerOfIVectorViewOfGattClientNotificationResult) error {
	_handler := uintptr(0)
	if handler != nil {
		_handler = handler.Ptr()
	}
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), _handler)
	return win32.ErrIfFailed(int32(r1))
}

// slot 7: get_Completed skipped: parameterized type Windows.Foundation.AsyncOperationCompletedHandler`1

// GetResults dispatches through IAsyncOperationOfIVectorViewOfGattClientNotificationResult's vtable slot 8.
func (self *IAsyncOperationOfIVectorViewOfGattClientNotificationResult) GetResults() (*IVectorViewOfGattClientNotificationResult, error) {
	result := new(*IVectorViewOfGattClientNotificationResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Await registers a Completed handler and blocks until IAsyncOperationOfIVectorViewOfGattClientNotificationResult reaches
// a terminal state, then returns GetResults() — or, when the status is not
// Completed, an error carrying the status and the IAsyncInfo error code (see
// winrt.AsyncError). Safe on an operation that already completed: WinRT
// invokes a handler assigned after completion immediately. put_Completed
// accepts a single assignment per operation, so Await (or SetCompleted) can
// be used at most once per instance. Await blocks indefinitely by design; a
// context-aware variant is future work. The completion signal is sent from
// the handler's Invoke, which the delegate runtime runs on a fresh goroutine
// — it never contends with the runtime's callback worker, so a completed
// operation cannot deadlock Await.
func (self *IAsyncOperationOfIVectorViewOfGattClientNotificationResult) Await() (*IVectorViewOfGattClientNotificationResult, error) {
	completion := make(chan foundation.AsyncStatus, 1)
	handler, err := NewAsyncOperationCompletedHandlerOfIVectorViewOfGattClientNotificationResult(func(_ *IAsyncOperationOfIVectorViewOfGattClientNotificationResult, asyncStatus foundation.AsyncStatus) {
		completion <- asyncStatus
	})
	if err != nil {
		return nil, err
	}
	defer handler.Close()
	if err := self.SetCompleted(handler); err != nil {
		return nil, err
	}
	status := <-completion
	if status != foundation.AsyncStatusCompleted {
		info, err := winrt.QueryInterface[foundation.IAsyncInfo](unsafe.Pointer(self), &foundation.IID_IAsyncInfo)
		if err != nil {
			return nil, err
		}
		defer info.Release()
		code, err := info.ErrorCode()
		if err != nil {
			return nil, err
		}
		return nil, winrt.AsyncError(int32(status), code)
	}
	return self.GetResults()
}

// IIterableOfGattCharacteristic is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>.
// IID: e3c56728-7f2d-5a0d-ad38-030d39c60f9f
type IIterableOfGattCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattCharacteristic is the interface identifier for IIterableOfGattCharacteristic.
var IID_IIterableOfGattCharacteristic = win32.GUID{Data1: 0xe3c56728, Data2: 0x7f2d, Data3: 0x5a0d, Data4: [8]byte{0xad, 0x38, 0x03, 0x0d, 0x39, 0xc6, 0x0f, 0x9f}}

// First dispatches through IIterableOfGattCharacteristic's vtable slot 6.
func (self *IIterableOfGattCharacteristic) First() (*IIteratorOfGattCharacteristic, error) {
	result := new(*IIteratorOfGattCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattCharacteristic creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattCharacteristic(items []*IGattCharacteristic) *IIterableOfGattCharacteristic {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattCharacteristic, Iterator: IID_IIteratorOfGattCharacteristic}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattCharacteristic)(unsafe.Pointer(obj))
}

// IIterableOfGattClientNotificationResult is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>.
// IID: d3f4b8ad-251f-5bbb-ada2-ea47434e74d6
type IIterableOfGattClientNotificationResult struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattClientNotificationResult is the interface identifier for IIterableOfGattClientNotificationResult.
var IID_IIterableOfGattClientNotificationResult = win32.GUID{Data1: 0xd3f4b8ad, Data2: 0x251f, Data3: 0x5bbb, Data4: [8]byte{0xad, 0xa2, 0xea, 0x47, 0x43, 0x4e, 0x74, 0xd6}}

// First dispatches through IIterableOfGattClientNotificationResult's vtable slot 6.
func (self *IIterableOfGattClientNotificationResult) First() (*IIteratorOfGattClientNotificationResult, error) {
	result := new(*IIteratorOfGattClientNotificationResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattClientNotificationResult creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattClientNotificationResult(items []*IGattClientNotificationResult) *IIterableOfGattClientNotificationResult {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattClientNotificationResult, Iterator: IID_IIteratorOfGattClientNotificationResult}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattClientNotificationResult)(unsafe.Pointer(obj))
}

// IIterableOfGattDescriptor is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>.
// IID: 30e2ffc4-3aa3-5219-9a18-ca2d0b6562e8
type IIterableOfGattDescriptor struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattDescriptor is the interface identifier for IIterableOfGattDescriptor.
var IID_IIterableOfGattDescriptor = win32.GUID{Data1: 0x30e2ffc4, Data2: 0x3aa3, Data3: 0x5219, Data4: [8]byte{0x9a, 0x18, 0xca, 0x2d, 0x0b, 0x65, 0x62, 0xe8}}

// First dispatches through IIterableOfGattDescriptor's vtable slot 6.
func (self *IIterableOfGattDescriptor) First() (*IIteratorOfGattDescriptor, error) {
	result := new(*IIteratorOfGattDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattDescriptor creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattDescriptor(items []*IGattDescriptor) *IIterableOfGattDescriptor {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattDescriptor, Iterator: IID_IIteratorOfGattDescriptor}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattDescriptor)(unsafe.Pointer(obj))
}

// IIterableOfGattDeviceService is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>.
// IID: 4b192e23-4893-56b2-8eff-439c3ab7fd1f
type IIterableOfGattDeviceService struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattDeviceService is the interface identifier for IIterableOfGattDeviceService.
var IID_IIterableOfGattDeviceService = win32.GUID{Data1: 0x4b192e23, Data2: 0x4893, Data3: 0x56b2, Data4: [8]byte{0x8e, 0xff, 0x43, 0x9c, 0x3a, 0xb7, 0xfd, 0x1f}}

// First dispatches through IIterableOfGattDeviceService's vtable slot 6.
func (self *IIterableOfGattDeviceService) First() (*IIteratorOfGattDeviceService, error) {
	result := new(*IIteratorOfGattDeviceService)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattDeviceService creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattDeviceService(items []*IGattDeviceService) *IIterableOfGattDeviceService {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattDeviceService, Iterator: IID_IIteratorOfGattDeviceService}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattDeviceService)(unsafe.Pointer(obj))
}

// IIterableOfGattLocalCharacteristic is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>.
// IID: 7082ed53-37f6-5262-8d09-939bea9edbd5
type IIterableOfGattLocalCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattLocalCharacteristic is the interface identifier for IIterableOfGattLocalCharacteristic.
var IID_IIterableOfGattLocalCharacteristic = win32.GUID{Data1: 0x7082ed53, Data2: 0x37f6, Data3: 0x5262, Data4: [8]byte{0x8d, 0x09, 0x93, 0x9b, 0xea, 0x9e, 0xdb, 0xd5}}

// First dispatches through IIterableOfGattLocalCharacteristic's vtable slot 6.
func (self *IIterableOfGattLocalCharacteristic) First() (*IIteratorOfGattLocalCharacteristic, error) {
	result := new(*IIteratorOfGattLocalCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattLocalCharacteristic creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattLocalCharacteristic(items []*IGattLocalCharacteristic) *IIterableOfGattLocalCharacteristic {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattLocalCharacteristic, Iterator: IID_IIteratorOfGattLocalCharacteristic}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattLocalCharacteristic)(unsafe.Pointer(obj))
}

// IIterableOfGattLocalDescriptor is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>.
// IID: 9016e9a2-c9f7-5d09-b0ae-552fa67796ae
type IIterableOfGattLocalDescriptor struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattLocalDescriptor is the interface identifier for IIterableOfGattLocalDescriptor.
var IID_IIterableOfGattLocalDescriptor = win32.GUID{Data1: 0x9016e9a2, Data2: 0xc9f7, Data3: 0x5d09, Data4: [8]byte{0xb0, 0xae, 0x55, 0x2f, 0xa6, 0x77, 0x96, 0xae}}

// First dispatches through IIterableOfGattLocalDescriptor's vtable slot 6.
func (self *IIterableOfGattLocalDescriptor) First() (*IIteratorOfGattLocalDescriptor, error) {
	result := new(*IIteratorOfGattLocalDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattLocalDescriptor creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattLocalDescriptor(items []*IGattLocalDescriptor) *IIterableOfGattLocalDescriptor {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattLocalDescriptor, Iterator: IID_IIteratorOfGattLocalDescriptor}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattLocalDescriptor)(unsafe.Pointer(obj))
}

// IIterableOfGattPresentationFormat is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
// IID: d75fcef1-c10e-5b7b-b130-f5a00314d35d
type IIterableOfGattPresentationFormat struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattPresentationFormat is the interface identifier for IIterableOfGattPresentationFormat.
var IID_IIterableOfGattPresentationFormat = win32.GUID{Data1: 0xd75fcef1, Data2: 0xc10e, Data3: 0x5b7b, Data4: [8]byte{0xb1, 0x30, 0xf5, 0xa0, 0x03, 0x14, 0xd3, 0x5d}}

// First dispatches through IIterableOfGattPresentationFormat's vtable slot 6.
func (self *IIterableOfGattPresentationFormat) First() (*IIteratorOfGattPresentationFormat, error) {
	result := new(*IIteratorOfGattPresentationFormat)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattPresentationFormat creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattPresentationFormat(items []*IGattPresentationFormat) *IIterableOfGattPresentationFormat {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattPresentationFormat, Iterator: IID_IIteratorOfGattPresentationFormat}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattPresentationFormat)(unsafe.Pointer(obj))
}

// IIterableOfGattSubscribedClient is the WinRT interface Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>.
// IID: b19864e4-f2b1-50de-8c11-fff9eca934e9
type IIterableOfGattSubscribedClient struct {
	syswinrt.IInspectable
}

// IID_IIterableOfGattSubscribedClient is the interface identifier for IIterableOfGattSubscribedClient.
var IID_IIterableOfGattSubscribedClient = win32.GUID{Data1: 0xb19864e4, Data2: 0xf2b1, Data3: 0x50de, Data4: [8]byte{0x8c, 0x11, 0xff, 0xf9, 0xec, 0xa9, 0x34, 0xe9}}

// First dispatches through IIterableOfGattSubscribedClient's vtable slot 6.
func (self *IIterableOfGattSubscribedClient) First() (*IIteratorOfGattSubscribedClient, error) {
	result := new(*IIteratorOfGattSubscribedClient)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// NewIIterableOfGattSubscribedClient creates a Go-implemented Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIIterableOfGattSubscribedClient(items []*IGattSubscribedClient) *IIterableOfGattSubscribedClient {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewIterableObject("Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattSubscribedClient, Iterator: IID_IIteratorOfGattSubscribedClient}, winrt.CodecInterface, boxed)
	return (*IIterableOfGattSubscribedClient)(unsafe.Pointer(obj))
}

// IIteratorOfGattCharacteristic is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>.
// IID: 1ffc4777-4346-5564-b7a5-59eae385f4f6
type IIteratorOfGattCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattCharacteristic is the interface identifier for IIteratorOfGattCharacteristic.
var IID_IIteratorOfGattCharacteristic = win32.GUID{Data1: 0x1ffc4777, Data2: 0x4346, Data3: 0x5564, Data4: [8]byte{0xb7, 0xa5, 0x59, 0xea, 0xe3, 0x85, 0xf4, 0xf6}}

// Current (propget get_Current) dispatches through IIteratorOfGattCharacteristic's vtable slot 6.
func (self *IIteratorOfGattCharacteristic) Current() (*IGattCharacteristic, error) {
	result := new(*IGattCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattCharacteristic's vtable slot 7.
func (self *IIteratorOfGattCharacteristic) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattCharacteristic's vtable slot 8.
func (self *IIteratorOfGattCharacteristic) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattClientNotificationResult is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>.
// IID: fca87386-a3ab-55e2-a83e-21f5bfab4049
type IIteratorOfGattClientNotificationResult struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattClientNotificationResult is the interface identifier for IIteratorOfGattClientNotificationResult.
var IID_IIteratorOfGattClientNotificationResult = win32.GUID{Data1: 0xfca87386, Data2: 0xa3ab, Data3: 0x55e2, Data4: [8]byte{0xa8, 0x3e, 0x21, 0xf5, 0xbf, 0xab, 0x40, 0x49}}

// Current (propget get_Current) dispatches through IIteratorOfGattClientNotificationResult's vtable slot 6.
func (self *IIteratorOfGattClientNotificationResult) Current() (*IGattClientNotificationResult, error) {
	result := new(*IGattClientNotificationResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattClientNotificationResult's vtable slot 7.
func (self *IIteratorOfGattClientNotificationResult) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattClientNotificationResult's vtable slot 8.
func (self *IIteratorOfGattClientNotificationResult) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattDescriptor is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>.
// IID: 3d8df436-cefb-5ffb-858c-4882ce1da079
type IIteratorOfGattDescriptor struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattDescriptor is the interface identifier for IIteratorOfGattDescriptor.
var IID_IIteratorOfGattDescriptor = win32.GUID{Data1: 0x3d8df436, Data2: 0xcefb, Data3: 0x5ffb, Data4: [8]byte{0x85, 0x8c, 0x48, 0x82, 0xce, 0x1d, 0xa0, 0x79}}

// Current (propget get_Current) dispatches through IIteratorOfGattDescriptor's vtable slot 6.
func (self *IIteratorOfGattDescriptor) Current() (*IGattDescriptor, error) {
	result := new(*IGattDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattDescriptor's vtable slot 7.
func (self *IIteratorOfGattDescriptor) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattDescriptor's vtable slot 8.
func (self *IIteratorOfGattDescriptor) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattDeviceService is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>.
// IID: 8beb3a26-73ca-50f3-a1d3-418c60a9f3b2
type IIteratorOfGattDeviceService struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattDeviceService is the interface identifier for IIteratorOfGattDeviceService.
var IID_IIteratorOfGattDeviceService = win32.GUID{Data1: 0x8beb3a26, Data2: 0x73ca, Data3: 0x50f3, Data4: [8]byte{0xa1, 0xd3, 0x41, 0x8c, 0x60, 0xa9, 0xf3, 0xb2}}

// Current (propget get_Current) dispatches through IIteratorOfGattDeviceService's vtable slot 6.
func (self *IIteratorOfGattDeviceService) Current() (*IGattDeviceService, error) {
	result := new(*IGattDeviceService)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattDeviceService's vtable slot 7.
func (self *IIteratorOfGattDeviceService) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattDeviceService's vtable slot 8.
func (self *IIteratorOfGattDeviceService) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattLocalCharacteristic is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>.
// IID: edd74875-8e85-546f-83b8-1e00aa896419
type IIteratorOfGattLocalCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattLocalCharacteristic is the interface identifier for IIteratorOfGattLocalCharacteristic.
var IID_IIteratorOfGattLocalCharacteristic = win32.GUID{Data1: 0xedd74875, Data2: 0x8e85, Data3: 0x546f, Data4: [8]byte{0x83, 0xb8, 0x1e, 0x00, 0xaa, 0x89, 0x64, 0x19}}

// Current (propget get_Current) dispatches through IIteratorOfGattLocalCharacteristic's vtable slot 6.
func (self *IIteratorOfGattLocalCharacteristic) Current() (*IGattLocalCharacteristic, error) {
	result := new(*IGattLocalCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattLocalCharacteristic's vtable slot 7.
func (self *IIteratorOfGattLocalCharacteristic) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattLocalCharacteristic's vtable slot 8.
func (self *IIteratorOfGattLocalCharacteristic) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattLocalDescriptor is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>.
// IID: 08023045-5f5c-59cc-abd3-bbcfe6fa7030
type IIteratorOfGattLocalDescriptor struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattLocalDescriptor is the interface identifier for IIteratorOfGattLocalDescriptor.
var IID_IIteratorOfGattLocalDescriptor = win32.GUID{Data1: 0x08023045, Data2: 0x5f5c, Data3: 0x59cc, Data4: [8]byte{0xab, 0xd3, 0xbb, 0xcf, 0xe6, 0xfa, 0x70, 0x30}}

// Current (propget get_Current) dispatches through IIteratorOfGattLocalDescriptor's vtable slot 6.
func (self *IIteratorOfGattLocalDescriptor) Current() (*IGattLocalDescriptor, error) {
	result := new(*IGattLocalDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattLocalDescriptor's vtable slot 7.
func (self *IIteratorOfGattLocalDescriptor) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattLocalDescriptor's vtable slot 8.
func (self *IIteratorOfGattLocalDescriptor) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattPresentationFormat is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
// IID: 20006c53-5dda-5319-91b1-c6f28fe65933
type IIteratorOfGattPresentationFormat struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattPresentationFormat is the interface identifier for IIteratorOfGattPresentationFormat.
var IID_IIteratorOfGattPresentationFormat = win32.GUID{Data1: 0x20006c53, Data2: 0x5dda, Data3: 0x5319, Data4: [8]byte{0x91, 0xb1, 0xc6, 0xf2, 0x8f, 0xe6, 0x59, 0x33}}

// Current (propget get_Current) dispatches through IIteratorOfGattPresentationFormat's vtable slot 6.
func (self *IIteratorOfGattPresentationFormat) Current() (*IGattPresentationFormat, error) {
	result := new(*IGattPresentationFormat)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattPresentationFormat's vtable slot 7.
func (self *IIteratorOfGattPresentationFormat) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattPresentationFormat's vtable slot 8.
func (self *IIteratorOfGattPresentationFormat) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IIteratorOfGattSubscribedClient is the WinRT interface Windows.Foundation.Collections.IIterator`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>.
// IID: df039115-a5ff-5d5a-b07b-20b42e078765
type IIteratorOfGattSubscribedClient struct {
	syswinrt.IInspectable
}

// IID_IIteratorOfGattSubscribedClient is the interface identifier for IIteratorOfGattSubscribedClient.
var IID_IIteratorOfGattSubscribedClient = win32.GUID{Data1: 0xdf039115, Data2: 0xa5ff, Data3: 0x5d5a, Data4: [8]byte{0xb0, 0x7b, 0x20, 0xb4, 0x2e, 0x07, 0x87, 0x65}}

// Current (propget get_Current) dispatches through IIteratorOfGattSubscribedClient's vtable slot 6.
func (self *IIteratorOfGattSubscribedClient) Current() (*IGattSubscribedClient, error) {
	result := new(*IGattSubscribedClient)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// HasCurrent (propget get_HasCurrent) dispatches through IIteratorOfGattSubscribedClient's vtable slot 7.
func (self *IIteratorOfGattSubscribedClient) HasCurrent() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// MoveNext dispatches through IIteratorOfGattSubscribedClient's vtable slot 8.
func (self *IIteratorOfGattSubscribedClient) MoveNext() (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// IReferenceOfUInt8 is the WinRT interface Windows.Foundation.IReference`1<UInt8>.
// IID: e5198cc8-2873-55f5-b0a1-84ff9e4aad62
// Requires: Windows.Foundation.IPropertyValue.
type IReferenceOfUInt8 struct {
	syswinrt.IInspectable
}

// IID_IReferenceOfUInt8 is the interface identifier for IReferenceOfUInt8.
var IID_IReferenceOfUInt8 = win32.GUID{Data1: 0xe5198cc8, Data2: 0x2873, Data3: 0x55f5, Data4: [8]byte{0xb0, 0xa1, 0x84, 0xff, 0x9e, 0x4a, 0xad, 0x62}}

// Value (propget get_Value) dispatches through IReferenceOfUInt8's vtable slot 6.
func (self *IReferenceOfUInt8) Value() (byte, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IVectorOfGattPresentationFormat is the WinRT interface Windows.Foundation.Collections.IVector`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
// IID: cba635ef-1c70-5412-8ede-7316276b9ee4
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
type IVectorOfGattPresentationFormat struct {
	syswinrt.IInspectable
}

// IID_IVectorOfGattPresentationFormat is the interface identifier for IVectorOfGattPresentationFormat.
var IID_IVectorOfGattPresentationFormat = win32.GUID{Data1: 0xcba635ef, Data2: 0x1c70, Data3: 0x5412, Data4: [8]byte{0x8e, 0xde, 0x73, 0x16, 0x27, 0x6b, 0x9e, 0xe4}}

// GetAt dispatches through IVectorOfGattPresentationFormat's vtable slot 6.
func (self *IVectorOfGattPresentationFormat) GetAt(index uint32) (*IGattPresentationFormat, error) {
	result := new(*IGattPresentationFormat)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorOfGattPresentationFormat's vtable slot 7.
func (self *IVectorOfGattPresentationFormat) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// GetView dispatches through IVectorOfGattPresentationFormat's vtable slot 8.
func (self *IVectorOfGattPresentationFormat) GetView() (*IVectorViewOfGattPresentationFormat, error) {
	result := new(*IVectorViewOfGattPresentationFormat)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorOfGattPresentationFormat's vtable slot 9.
func (self *IVectorOfGattPresentationFormat) IndexOf(value *IGattPresentationFormat, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[9], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// SetAt dispatches through IVectorOfGattPresentationFormat's vtable slot 10.
func (self *IVectorOfGattPresentationFormat) SetAt(index uint32, value *IGattPresentationFormat) error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[10], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(unsafe.Pointer(value)))
	return win32.ErrIfFailed(int32(r1))
}

// InsertAt dispatches through IVectorOfGattPresentationFormat's vtable slot 11.
func (self *IVectorOfGattPresentationFormat) InsertAt(index uint32, value *IGattPresentationFormat) error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[11], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(unsafe.Pointer(value)))
	return win32.ErrIfFailed(int32(r1))
}

// RemoveAt dispatches through IVectorOfGattPresentationFormat's vtable slot 12.
func (self *IVectorOfGattPresentationFormat) RemoveAt(index uint32) error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[12], uintptr(unsafe.Pointer(self)), uintptr(index))
	return win32.ErrIfFailed(int32(r1))
}

// Append dispatches through IVectorOfGattPresentationFormat's vtable slot 13.
func (self *IVectorOfGattPresentationFormat) Append(value *IGattPresentationFormat) error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[13], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)))
	return win32.ErrIfFailed(int32(r1))
}

// RemoveAtEnd dispatches through IVectorOfGattPresentationFormat's vtable slot 14.
func (self *IVectorOfGattPresentationFormat) RemoveAtEnd() error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[14], uintptr(unsafe.Pointer(self)))
	return win32.ErrIfFailed(int32(r1))
}

// Clear dispatches through IVectorOfGattPresentationFormat's vtable slot 15.
func (self *IVectorOfGattPresentationFormat) Clear() error {
	r1, _, _ := syscall.SyscallN(self.LpVtbl[15], uintptr(unsafe.Pointer(self)))
	return win32.ErrIfFailed(int32(r1))
}

// slot 16: GetMany skipped: conformant array

// slot 17: ReplaceAll skipped: conformant array

// NewIVectorOfGattPresentationFormat creates a Go-implemented Windows.Foundation.Collections.IVector`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
// The vector is writable through the WinRT ABI (the Go side exposes no
// mutation API); GetView returns an immutable SNAPSHOT of the contents at
// call time.
func NewIVectorOfGattPresentationFormat(items []*IGattPresentationFormat) *IVectorOfGattPresentationFormat {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorObject("Windows.Foundation.Collections.IVector`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattPresentationFormat, Iterator: IID_IIteratorOfGattPresentationFormat, VectorView: IID_IVectorViewOfGattPresentationFormat, Vector: IID_IVectorOfGattPresentationFormat}, winrt.CodecInterface, boxed)
	return (*IVectorOfGattPresentationFormat)(unsafe.Pointer(obj))
}

// IVectorViewOfGattCharacteristic is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>.
// IID: cb3ab3ae-b561-504f-a808-599deceb2df4
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>.
type IVectorViewOfGattCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattCharacteristic is the interface identifier for IVectorViewOfGattCharacteristic.
var IID_IVectorViewOfGattCharacteristic = win32.GUID{Data1: 0xcb3ab3ae, Data2: 0xb561, Data3: 0x504f, Data4: [8]byte{0xa8, 0x08, 0x59, 0x9d, 0xec, 0xeb, 0x2d, 0xf4}}

// GetAt dispatches through IVectorViewOfGattCharacteristic's vtable slot 6.
func (self *IVectorViewOfGattCharacteristic) GetAt(index uint32) (*IGattCharacteristic, error) {
	result := new(*IGattCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattCharacteristic's vtable slot 7.
func (self *IVectorViewOfGattCharacteristic) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattCharacteristic's vtable slot 8.
func (self *IVectorViewOfGattCharacteristic) IndexOf(value *IGattCharacteristic, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattCharacteristic creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattCharacteristic(items []*IGattCharacteristic) *IVectorViewOfGattCharacteristic {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattCharacteristic>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattCharacteristic, Iterator: IID_IIteratorOfGattCharacteristic, VectorView: IID_IVectorViewOfGattCharacteristic}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattCharacteristic)(unsafe.Pointer(obj))
}

// IVectorViewOfGattClientNotificationResult is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>.
// IID: c886eb62-ec71-586b-a158-66dc62a378b7
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>.
type IVectorViewOfGattClientNotificationResult struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattClientNotificationResult is the interface identifier for IVectorViewOfGattClientNotificationResult.
var IID_IVectorViewOfGattClientNotificationResult = win32.GUID{Data1: 0xc886eb62, Data2: 0xec71, Data3: 0x586b, Data4: [8]byte{0xa1, 0x58, 0x66, 0xdc, 0x62, 0xa3, 0x78, 0xb7}}

// GetAt dispatches through IVectorViewOfGattClientNotificationResult's vtable slot 6.
func (self *IVectorViewOfGattClientNotificationResult) GetAt(index uint32) (*IGattClientNotificationResult, error) {
	result := new(*IGattClientNotificationResult)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattClientNotificationResult's vtable slot 7.
func (self *IVectorViewOfGattClientNotificationResult) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattClientNotificationResult's vtable slot 8.
func (self *IVectorViewOfGattClientNotificationResult) IndexOf(value *IGattClientNotificationResult, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattClientNotificationResult creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattClientNotificationResult(items []*IGattClientNotificationResult) *IVectorViewOfGattClientNotificationResult {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattClientNotificationResult>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattClientNotificationResult, Iterator: IID_IIteratorOfGattClientNotificationResult, VectorView: IID_IVectorViewOfGattClientNotificationResult}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattClientNotificationResult)(unsafe.Pointer(obj))
}

// IVectorViewOfGattDescriptor is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>.
// IID: 19605ea8-73d6-5760-849b-fe5f8a2bd05c
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>.
type IVectorViewOfGattDescriptor struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattDescriptor is the interface identifier for IVectorViewOfGattDescriptor.
var IID_IVectorViewOfGattDescriptor = win32.GUID{Data1: 0x19605ea8, Data2: 0x73d6, Data3: 0x5760, Data4: [8]byte{0x84, 0x9b, 0xfe, 0x5f, 0x8a, 0x2b, 0xd0, 0x5c}}

// GetAt dispatches through IVectorViewOfGattDescriptor's vtable slot 6.
func (self *IVectorViewOfGattDescriptor) GetAt(index uint32) (*IGattDescriptor, error) {
	result := new(*IGattDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattDescriptor's vtable slot 7.
func (self *IVectorViewOfGattDescriptor) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattDescriptor's vtable slot 8.
func (self *IVectorViewOfGattDescriptor) IndexOf(value *IGattDescriptor, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattDescriptor creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattDescriptor(items []*IGattDescriptor) *IVectorViewOfGattDescriptor {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDescriptor>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattDescriptor, Iterator: IID_IIteratorOfGattDescriptor, VectorView: IID_IVectorViewOfGattDescriptor}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattDescriptor)(unsafe.Pointer(obj))
}

// IVectorViewOfGattDeviceService is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>.
// IID: 7c8e7fdd-a1a1-528a-81d1-296769227a08
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>.
type IVectorViewOfGattDeviceService struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattDeviceService is the interface identifier for IVectorViewOfGattDeviceService.
var IID_IVectorViewOfGattDeviceService = win32.GUID{Data1: 0x7c8e7fdd, Data2: 0xa1a1, Data3: 0x528a, Data4: [8]byte{0x81, 0xd1, 0x29, 0x67, 0x69, 0x22, 0x7a, 0x08}}

// GetAt dispatches through IVectorViewOfGattDeviceService's vtable slot 6.
func (self *IVectorViewOfGattDeviceService) GetAt(index uint32) (*IGattDeviceService, error) {
	result := new(*IGattDeviceService)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattDeviceService's vtable slot 7.
func (self *IVectorViewOfGattDeviceService) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattDeviceService's vtable slot 8.
func (self *IVectorViewOfGattDeviceService) IndexOf(value *IGattDeviceService, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattDeviceService creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattDeviceService(items []*IGattDeviceService) *IVectorViewOfGattDeviceService {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattDeviceService>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattDeviceService, Iterator: IID_IIteratorOfGattDeviceService, VectorView: IID_IVectorViewOfGattDeviceService}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattDeviceService)(unsafe.Pointer(obj))
}

// IVectorViewOfGattLocalCharacteristic is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>.
// IID: e4865eba-6de3-5a99-9a75-7efd8e3cb096
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>.
type IVectorViewOfGattLocalCharacteristic struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattLocalCharacteristic is the interface identifier for IVectorViewOfGattLocalCharacteristic.
var IID_IVectorViewOfGattLocalCharacteristic = win32.GUID{Data1: 0xe4865eba, Data2: 0x6de3, Data3: 0x5a99, Data4: [8]byte{0x9a, 0x75, 0x7e, 0xfd, 0x8e, 0x3c, 0xb0, 0x96}}

// GetAt dispatches through IVectorViewOfGattLocalCharacteristic's vtable slot 6.
func (self *IVectorViewOfGattLocalCharacteristic) GetAt(index uint32) (*IGattLocalCharacteristic, error) {
	result := new(*IGattLocalCharacteristic)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattLocalCharacteristic's vtable slot 7.
func (self *IVectorViewOfGattLocalCharacteristic) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattLocalCharacteristic's vtable slot 8.
func (self *IVectorViewOfGattLocalCharacteristic) IndexOf(value *IGattLocalCharacteristic, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattLocalCharacteristic creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattLocalCharacteristic(items []*IGattLocalCharacteristic) *IVectorViewOfGattLocalCharacteristic {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalCharacteristic>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattLocalCharacteristic, Iterator: IID_IIteratorOfGattLocalCharacteristic, VectorView: IID_IVectorViewOfGattLocalCharacteristic}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattLocalCharacteristic)(unsafe.Pointer(obj))
}

// IVectorViewOfGattLocalDescriptor is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>.
// IID: 7f4688cc-0bbc-5070-8974-19fcb1acbf6c
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>.
type IVectorViewOfGattLocalDescriptor struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattLocalDescriptor is the interface identifier for IVectorViewOfGattLocalDescriptor.
var IID_IVectorViewOfGattLocalDescriptor = win32.GUID{Data1: 0x7f4688cc, Data2: 0x0bbc, Data3: 0x5070, Data4: [8]byte{0x89, 0x74, 0x19, 0xfc, 0xb1, 0xac, 0xbf, 0x6c}}

// GetAt dispatches through IVectorViewOfGattLocalDescriptor's vtable slot 6.
func (self *IVectorViewOfGattLocalDescriptor) GetAt(index uint32) (*IGattLocalDescriptor, error) {
	result := new(*IGattLocalDescriptor)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattLocalDescriptor's vtable slot 7.
func (self *IVectorViewOfGattLocalDescriptor) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattLocalDescriptor's vtable slot 8.
func (self *IVectorViewOfGattLocalDescriptor) IndexOf(value *IGattLocalDescriptor, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattLocalDescriptor creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattLocalDescriptor(items []*IGattLocalDescriptor) *IVectorViewOfGattLocalDescriptor {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattLocalDescriptor>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattLocalDescriptor, Iterator: IID_IIteratorOfGattLocalDescriptor, VectorView: IID_IVectorViewOfGattLocalDescriptor}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattLocalDescriptor)(unsafe.Pointer(obj))
}

// IVectorViewOfGattPresentationFormat is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
// IID: 0ea2c154-22b8-5c8e-925d-d47e1aad31bb
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>.
type IVectorViewOfGattPresentationFormat struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattPresentationFormat is the interface identifier for IVectorViewOfGattPresentationFormat.
var IID_IVectorViewOfGattPresentationFormat = win32.GUID{Data1: 0x0ea2c154, Data2: 0x22b8, Data3: 0x5c8e, Data4: [8]byte{0x92, 0x5d, 0xd4, 0x7e, 0x1a, 0xad, 0x31, 0xbb}}

// GetAt dispatches through IVectorViewOfGattPresentationFormat's vtable slot 6.
func (self *IVectorViewOfGattPresentationFormat) GetAt(index uint32) (*IGattPresentationFormat, error) {
	result := new(*IGattPresentationFormat)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattPresentationFormat's vtable slot 7.
func (self *IVectorViewOfGattPresentationFormat) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattPresentationFormat's vtable slot 8.
func (self *IVectorViewOfGattPresentationFormat) IndexOf(value *IGattPresentationFormat, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattPresentationFormat creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattPresentationFormat(items []*IGattPresentationFormat) *IVectorViewOfGattPresentationFormat {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattPresentationFormat>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattPresentationFormat, Iterator: IID_IIteratorOfGattPresentationFormat, VectorView: IID_IVectorViewOfGattPresentationFormat}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattPresentationFormat)(unsafe.Pointer(obj))
}

// IVectorViewOfGattSubscribedClient is the WinRT interface Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>.
// IID: 63391d79-4ba7-5f45-9681-3a683089353b
// Requires: Windows.Foundation.Collections.IIterable`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>.
type IVectorViewOfGattSubscribedClient struct {
	syswinrt.IInspectable
}

// IID_IVectorViewOfGattSubscribedClient is the interface identifier for IVectorViewOfGattSubscribedClient.
var IID_IVectorViewOfGattSubscribedClient = win32.GUID{Data1: 0x63391d79, Data2: 0x4ba7, Data3: 0x5f45, Data4: [8]byte{0x96, 0x81, 0x3a, 0x68, 0x30, 0x89, 0x35, 0x3b}}

// GetAt dispatches through IVectorViewOfGattSubscribedClient's vtable slot 6.
func (self *IVectorViewOfGattSubscribedClient) GetAt(index uint32) (*IGattSubscribedClient, error) {
	result := new(*IGattSubscribedClient)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[6], uintptr(unsafe.Pointer(self)), uintptr(index), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// Size (propget get_Size) dispatches through IVectorViewOfGattSubscribedClient's vtable slot 7.
func (self *IVectorViewOfGattSubscribedClient) Size() (uint32, error) {
	result := new(uint32)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[7], uintptr(unsafe.Pointer(self)), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result, win32.ErrIfFailed(int32(r1))
}

// IndexOf dispatches through IVectorViewOfGattSubscribedClient's vtable slot 8.
func (self *IVectorViewOfGattSubscribedClient) IndexOf(value *IGattSubscribedClient, index *uint32) (bool, error) {
	result := new(byte)
	r1, _, _ := syscall.SyscallN(self.LpVtbl[8], uintptr(unsafe.Pointer(self)), uintptr(unsafe.Pointer(value)), uintptr(winrt.OutParam(unsafe.Pointer(index))), uintptr(winrt.OutParam(unsafe.Pointer(result))))
	return *result != 0, win32.ErrIfFailed(int32(r1))
}

// slot 9: GetMany skipped: conformant array

// NewIVectorViewOfGattSubscribedClient creates a Go-implemented Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>
// over items, for passing INTO WinRT methods that consume the collection —
// native code drives it through Go-implemented vtables (see the runtime's
// collection core). The object starts with one caller-owned reference:
// Release it (through the embedded IInspectable) once no native code can
// still hold it.
// Items are BORROWED: the collection AddRefs each element and releases it
// as it is displaced, removed, or when the collection itself is released.
// IndexOf compares COM identity WORDS (no QueryInterface is issued): an
// element matches only the exact interface pointer it was built from.
func NewIVectorViewOfGattSubscribedClient(items []*IGattSubscribedClient) *IVectorViewOfGattSubscribedClient {
	boxed := make([]any, len(items))
	for i, item := range items {
		boxed[i] = uintptr(unsafe.Pointer(item))
	}
	obj := winrt.NewVectorViewObject("Windows.Foundation.Collections.IVectorView`1<Windows.Devices.Bluetooth.GenericAttributeProfile.GattSubscribedClient>", winrt.CollectionIIDs{Iterable: IID_IIterableOfGattSubscribedClient, Iterator: IID_IIteratorOfGattSubscribedClient, VectorView: IID_IVectorViewOfGattSubscribedClient}, winrt.CodecInterface, boxed)
	return (*IVectorViewOfGattSubscribedClient)(unsafe.Pointer(obj))
}
