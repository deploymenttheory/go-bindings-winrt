// Code generated by go-bindings-winrt-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package popups

import (
	"unsafe"

	"github.com/deploymenttheory/go-bindings-win32/bindings/runtime/win32"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/runtime/winrt"
	"github.com/deploymenttheory/go-bindings-winrt/bindings/winrt/foundation"
)

// AsyncOperationCompletedHandlerOfIUICommand is a Go-implemented handler for the WinRT delegate
// Windows.Foundation.AsyncOperationCompletedHandler`1<Windows.UI.Popups.IUICommand>.
// IID: dd33fd5b-a24d-5a44-91fe-dd6441770103
type AsyncOperationCompletedHandlerOfIUICommand struct {
	delegate *winrt.Delegate
}

// IID_AsyncOperationCompletedHandlerOfIUICommand is the delegate identifier for AsyncOperationCompletedHandlerOfIUICommand.
var IID_AsyncOperationCompletedHandlerOfIUICommand = win32.GUID{Data1: 0xdd33fd5b, Data2: 0xa24d, Data3: 0x5a44, Data4: [8]byte{0x91, 0xfe, 0xdd, 0x64, 0x41, 0x77, 0x01, 0x03}}

// NewAsyncOperationCompletedHandlerOfIUICommand wraps fn as a COM-callable Windows.Foundation.AsyncOperationCompletedHandler`1<Windows.UI.Popups.IUICommand>.
// The handler starts with one Go-held reference; Close it once no native
// code can still invoke it.
// Pointer-typed callback arguments are BORROWED references owned by the
// event source for the duration of the callback: do not Release them or
// retain them past its return.
func NewAsyncOperationCompletedHandlerOfIUICommand(fn func(asyncInfo *IAsyncOperationOfIUICommand, asyncStatus foundation.AsyncStatus)) (*AsyncOperationCompletedHandlerOfIUICommand, error) {
	delegate, err := winrt.NewDelegate(IID_AsyncOperationCompletedHandlerOfIUICommand, 2, func(raw []uintptr) uintptr {
		fn((*IAsyncOperationOfIUICommand)(unsafe.Pointer(raw[0])), foundation.AsyncStatus(raw[1]))
		return 0
	})
	if err != nil {
		return nil, err
	}
	return &AsyncOperationCompletedHandlerOfIUICommand{delegate: delegate}, nil
}

// Ptr is the COM object pointer an Add<Event> method registers.
func (h *AsyncOperationCompletedHandlerOfIUICommand) Ptr() uintptr { return h.delegate.Ptr() }

// Close releases the Go-held reference. Call it once no native code can
// still invoke the handler (after the event source removed it or closed);
// the runtime keeps its own references while the handler stays registered.
func (h *AsyncOperationCompletedHandlerOfIUICommand) Close() { h.delegate.Release() }

// UICommandInvokedHandler is a Go-implemented handler for the WinRT delegate
// Windows.UI.Popups.UICommandInvokedHandler.
// IID: daf77a4f-c27a-4298-9ac6-2922c45e7da6
type UICommandInvokedHandler struct {
	delegate *winrt.Delegate
}

// IID_UICommandInvokedHandler is the delegate identifier for UICommandInvokedHandler.
var IID_UICommandInvokedHandler = win32.GUID{Data1: 0xdaf77a4f, Data2: 0xc27a, Data3: 0x4298, Data4: [8]byte{0x9a, 0xc6, 0x29, 0x22, 0xc4, 0x5e, 0x7d, 0xa6}}

// NewUICommandInvokedHandler wraps fn as a COM-callable Windows.UI.Popups.UICommandInvokedHandler.
// The handler starts with one Go-held reference; Close it once no native
// code can still invoke it.
// Pointer-typed callback arguments are BORROWED references owned by the
// event source for the duration of the callback: do not Release them or
// retain them past its return.
func NewUICommandInvokedHandler(fn func(command *IUICommand)) (*UICommandInvokedHandler, error) {
	delegate, err := winrt.NewDelegate(IID_UICommandInvokedHandler, 1, func(raw []uintptr) uintptr {
		fn((*IUICommand)(unsafe.Pointer(raw[0])))
		return 0
	})
	if err != nil {
		return nil, err
	}
	return &UICommandInvokedHandler{delegate: delegate}, nil
}

// Ptr is the COM object pointer an Add<Event> method registers.
func (h *UICommandInvokedHandler) Ptr() uintptr { return h.delegate.Ptr() }

// Close releases the Go-held reference. Call it once no native code can
// still invoke the handler (after the event source removed it or closed);
// the runtime keeps its own references while the handler stays registered.
func (h *UICommandInvokedHandler) Close() { h.delegate.Release() }
